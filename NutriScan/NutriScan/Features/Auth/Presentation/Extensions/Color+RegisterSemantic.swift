//
//  Color+RegisterSemantic.swift
//  NutriScan
//

import SwiftUI

// MARK: - Register Semantic Colors
extension Color {
    struct RegisterSemantic {

        // MARK: Header
        static let headerBackground = Color.Teal.teal800
        static let headerTitle = Color.Teal.teal200

        // MARK: Footer ("Already have an account?")
        static let footerText = Color(light: Color.Gray.gray1000, dark: Color.Gray.gray400)
        static let footerLink = Color.Teal.teal1000
    }
}
