//
//  CaloriesHistorySemantic.swift
//  NutriScan
//
//  Created by albaraa alsayed on 20/02/1448 AH.
//

import SwiftUI

extension Color {
    enum CaloriesHistorySemantic {
        static let background = Color(
            light: .white,
            dark: Color.Teal.teal1600
        )
        static let title = Color(
            light: Color.Gray.gray1600,
            dark: Color.Teal.teal1000
        )
        static let calendarButtonBackground = Color(
            light: Color.Teal.teal300,
            dark: Color.Teal.teal1500
        )
        static let dayBackground = Color(
            light: Color.Gray.gray200,
            dark: Color.Teal.teal1500
        )
        static let dateBackground = Color(
            light: .white,
            dark: Color.Teal.teal1500
        )
        static let dateText = Color(
            light: Color.Gray.gray1000,
            dark: Color.Teal.teal1000
        )
        static let metricBackground = Color(
            light: .white,
            dark: Color.Teal.teal1600
        )
        static let metricTitle = Color.Teal.teal1000
        static let metricValue = Color(
            light: .black,
            dark: Color.Teal.teal700
        )
        static let metricUnit = Color(
            light: Color.Gray.gray600,
            dark: Color.Teal.teal1300
        )
        static let iconBackground = Color(
            light: Color.Teal.teal200,
            dark: Color.Teal.teal1400
        )
        static let shimmerPlaceholder = Color(
            light: Color.Gray.gray300,
            dark: Color.Teal.teal1400
        )
    }
}
